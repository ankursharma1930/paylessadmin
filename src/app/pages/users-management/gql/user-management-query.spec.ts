import { UserManagementQuery } from './user-management-query';

describe('UserManagementQuery', () => {
  it('should create an instance', () => {
    expect(new UserManagementQuery()).toBeTruthy();
  });
});
